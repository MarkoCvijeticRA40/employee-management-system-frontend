import { LogNotificationEntity } from './log-notification-entity.model';

describe('LogNotificationEntity', () => {
  it('should create an instance', () => {
    expect(new LogNotificationEntity()).toBeTruthy();
  });
});
