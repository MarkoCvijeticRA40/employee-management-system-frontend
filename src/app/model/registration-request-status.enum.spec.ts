import { RegistrationRequestStatus } from './registration-request-status.enum';

describe('RegistrationRequestStatus', () => {
  it('should create an instance', () => {
    expect(new RegistrationRequestStatus()).toBeTruthy();
  });
});
